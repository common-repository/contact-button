(function( $ ) {
	'use strict';

	/**
	 * All of the code for admin-facing JavaScript source
	 * reside in this file.
	*/
	 
	// When the window is loaded:
	$( window ).load(function() {
        //todo
	});

})( jQuery );
